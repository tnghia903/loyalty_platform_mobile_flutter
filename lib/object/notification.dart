class Notification {
  final int id;
  final String icon;
  final String tilte;
  final String description;
  final String datetime;
  final bool isRead;
  Notification(this.id, this.icon, this.tilte, this.description, this.datetime,
      this.isRead);
}
